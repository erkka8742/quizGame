// Change this to your server's IP address
// Find it by running the backend - it shows the IP in the console
export const WS_HOST = '10.30.18.68';
export const WS_PORT = 7654;  // WebSocket server port (not 5173 which is Vite)
export const WS_URL = `ws://${WS_HOST}:${WS_PORT}`;
